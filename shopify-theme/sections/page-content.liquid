<div class="container py-5" style="padding-top:130px!important;min-height:60vh;">
  <div class="row justify-content-center">
    <div class="col-lg-9">
      <h1 class="font-serif mb-4" style="font-size:clamp(2rem,4vw,3rem);color:var(--text-dark);">{{ page.title }}</h1>
      <div class="text-muted" style="font-size:.95rem;line-height:1.8;">{{ page.content }}</div>
    </div>
  </div>
</div>

{% schema %}
{ "name": "Page Content", "settings": [] }
{% endschema %}
